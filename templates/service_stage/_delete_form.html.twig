<form method="post" action="{{ path('service_stage_delete', {'id': service_stage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ service_stage.id) }}">
    <button class="btn">eliminar</button>
</form>
